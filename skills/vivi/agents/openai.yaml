display_name: Mail
short_description: Work with email through Vivi—IMAP, search, drafts, queues, and project mailspaces (task/need/want).
default_prompt: Use this skill for Vivi email and project mailspace CLI. Verify the live `vivi` surface first; do not parse cache files. Treat external send/mutation as approval-gated via exec, enqueue, and queue.
