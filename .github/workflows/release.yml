name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-apple-darwin
            os: macos-latest

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        run: |
          rustup target add ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package binary
        run: |
          cd target/${{ matrix.target }}/release
          tar czf vivi-${{ matrix.target }}.tar.gz vivi
          mv vivi-${{ matrix.target }}.tar.gz ../../../

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: vivi-${{ matrix.target }}
          path: vivi-${{ matrix.target }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            vivi-aarch64-apple-darwin/vivi-aarch64-apple-darwin.tar.gz
            vivi-x86_64-apple-darwin/vivi-x86_64-apple-darwin.tar.gz
          generate_release_notes: true

  update-tap:
    needs: release
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8

      - name: Compute SHA256 checksums
        id: sha
        run: |
          echo "arm64=$(sha256sum vivi-aarch64-apple-darwin/vivi-aarch64-apple-darwin.tar.gz | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
          echo "x86_64=$(sha256sum vivi-x86_64-apple-darwin/vivi-x86_64-apple-darwin.tar.gz | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Generate formula
        run: |
          cat > vivarium.rb << 'FORMULA'
          class Vivarium < Formula
            desc "Local-first IMAP email sync for LLMs"
            homepage "https://github.com/ianzepp/vivarium"
            license "MIT"

            on_macos do
              if Hardware::CPU.arm?
                url "https://github.com/ianzepp/vivarium/releases/download/${{ github.ref_name }}/vivi-aarch64-apple-darwin.tar.gz"
                sha256 "${{ steps.sha.outputs.arm64 }}"
              else
                url "https://github.com/ianzepp/vivarium/releases/download/${{ github.ref_name }}/vivi-x86_64-apple-darwin.tar.gz"
                sha256 "${{ steps.sha.outputs.x86_64 }}"
              end
            end

            def install
              bin.install "vivi"
            end

            test do
              assert_match version.to_s, shell_output("#{bin}/vivi --version")
              assert_match "send", shell_output("#{bin}/vivi --help")
            end
          end
          FORMULA

      - name: Push formula to tap
        uses: dmnemec/copy_file_to_another_repo_action@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.TAP_GITHUB_TOKEN }}
        with:
          source_file: vivarium.rb
          destination_repo: ianzepp/homebrew-tap
          destination_folder: Formula
          user_email: github-actions[bot]@users.noreply.github.com
          user_name: github-actions[bot]
          commit_message: "Update vivarium to ${{ steps.version.outputs.version }}"
